# Required: Anthropic API key for the chatbot
ANTHROPIC_API_KEY=sk-ant-...

# Required for semantic search: OpenAI API key for embeddings
# Only used during `npm run ingest` and at query time.
# Without this, the app falls back to keyword search.
OPENAI_API_KEY=sk-...
